# Copy this file to .env and fill in your real key to enable the AI
# skill-gap recommendation feature. Without it, the app still works -
# the skill-gap tool just falls back to a rule-based (non-AI) response.
#
# Get a key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-api-key-here
